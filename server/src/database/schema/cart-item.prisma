model CartItem {
    id     Int  @id @default(autoincrement())
    cartId Int  @unique @map("card_id")
    cart   Cart @relation(fields: [cartId], references: [id], onDelete: Cascade)

    @@map("cart_items")
}