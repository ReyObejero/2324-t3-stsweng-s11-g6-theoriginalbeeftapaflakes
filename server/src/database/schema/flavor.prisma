model Flavor {
    id           Int             @id @default(autoincrement())
    name         String          @unique
    variants     FlavorVariant[]
    packageItems PackageItem[]

    @@map("flavors")
}