model Product {
    id          Int       @id @default(autoincrement())
    name        String    @unique
    description String
    ingredients String
    imageUrl    String
    numReviews  Int       @default(0) @map("num_reviews")
    rating      Float     @default(0.0)
    packages    Package[]

    @@map("products")
}